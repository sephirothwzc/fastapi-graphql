[tool.poetry]
name = "fastapi-graphql"
version = "0.1.0"
description = ""
authors = ["zhanchao.wu <zhanchao.wu@icloud.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
